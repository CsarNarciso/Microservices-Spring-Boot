spring:
  application:
    name: msvc-editorials
    
  config:
    import: optional:configserver:lb://msvc-configServer
  
  cloud:
    config:
      discovery:
        serviceId: msvc-configServer
        enabled: true
#Postgres database
  datasource:
    driverClassName: org.postgresql.Driver 
    url: jdbc:microservices-project-postgresSQL-server://microservices-project-db:5543/editorials
    username: root
    password: letmein 
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect:
        format-sql: true 
      show-sql: true
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl: 
      defaultZone: http://msvc-eureka:8761/eureka/
